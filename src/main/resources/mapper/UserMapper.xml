<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjq.mapper.UserMapper">
    <select id="findByUsername" resultType="com.kjq.pojo.User">
        SELECT * FROM user
        where
            username=#{username}
    </select>
    <select id="findUserById" resultType="com.kjq.pojo.User">
        SELECT * FROM user
        where
            id=#{Id}
    </select>
    <insert id="addUser" useGeneratedKeys="true" keyProperty="id" parameterType="com.kjq.pojo.User">
        insert into user(username,password)
        values(#{username},#{password})
    </insert>
</mapper>